# 任务聊天室 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为每个开启了 `partnerChat` 的任务添加独立的实时文字聊天室，仅任务参与者可用，任务结束后聊天室自动关闭并删除消息。

**Architecture:** 在现有 Express 服务器基础上，用 `http.createServer` 包裹 app，并挂载 `ws` WebSocket 服务器共享同一端口。后端新增 `backend/src/chat.js` 管理房间和鉴权，前端新增 `miniprogram/pages/chat/` 聊天室页面。

**Tech Stack:** Node.js `ws@^8`, MySQL（新增 `messages` 表）, 微信小程序原生 `wx.connectSocket` API

---

## 文件结构

| 操作 | 文件 | 说明 |
|------|------|------|
| 新增 | `backend/src/chat.js` | WebSocket 服务器：房间管理、鉴权、消息广播、房间关闭 |
| 修改 | `backend/src/db.js` | 新增 `messages` 表 DDL + `insertMessage` / `getRecentMessages` / `deleteMessagesByPost` 查询函数 |
| 修改 | `backend/src/server.js` | 升级为 `http.createServer`，挂载 chat.js；在 complete/abandon 路由调用 `chatServer.closeRoom` |
| 新增 | `miniprogram/pages/chat/chat.js` | 聊天室页面逻辑 |
| 新增 | `miniprogram/pages/chat/chat.wxml` | 聊天室页面 UI |
| 新增 | `miniprogram/pages/chat/chat.wxss` | 聊天室页面样式 |
| 新增 | `miniprogram/pages/chat/chat.json` | 聊天室页面配置 |
| 修改 | `miniprogram/app.json` | 注册 `pages/chat/chat` 路由 |
| 修改 | `miniprogram/pages/post-detail/post-detail.wxml` | 增加"进入聊天室"按钮 |
| 修改 | `miniprogram/pages/post-detail/post-detail.js` | 增加 `openChat()` 方法 |
| 修改 | `miniprogram/services/api.js` | 增加 `getChatHistory(postId, userId)` |

---

## Task 1: 安装 ws 依赖

**Files:**
- Modify: `backend/package.json`

- [ ] **Step 1: 安装依赖**

```bash
cd backend && npm install ws@^8.18.0
```

Expected output: `added 1 package` （或类似，无 error）

- [ ] **Step 2: 确认 package.json 中出现 ws**

```bash
grep '"ws"' backend/package.json
```

Expected: `"ws": "^8.18.0"` 或类似版本行

- [ ] **Step 3: Commit**

```bash
git add backend/package.json backend/package-lock.json
git commit -m "chore: install ws websocket package"
```

---

## Task 2: 数据库 — 新增 messages 表及查询函数

**Files:**
- Modify: `backend/src/db.js`

- [ ] **Step 1: 在 `createTables()` 末尾追加 messages 表 DDL**

在 `backend/src/db.js` 的 `createTables` 函数末尾，紧接在最后一个 `await query(...)` 之后，添加：

```js
  await query(`
    CREATE TABLE IF NOT EXISTS messages (
      id          INT AUTO_INCREMENT PRIMARY KEY,
      postId      VARCHAR(64) NOT NULL,
      senderId    VARCHAR(64) NOT NULL,
      senderName  VARCHAR(100) NOT NULL,
      content     TEXT NOT NULL,
      createdAt   DATETIME DEFAULT CURRENT_TIMESTAMP,
      INDEX idx_messages_postId_createdAt (postId, createdAt)
    ) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4
  `);
```

- [ ] **Step 2: 在 `module.exports` 之前添加三个查询函数**

```js
async function insertMessage(postId, senderId, senderName, content) {
  const [result] = await createPool().execute(
    'INSERT INTO messages (postId, senderId, senderName, content) VALUES (?, ?, ?, ?)',
    [postId, senderId, senderName, content]
  );
  return result.insertId;
}

async function getRecentMessages(postId, limit = 50) {
  const rows = await query(
    `SELECT id, senderId, senderName, content, createdAt
     FROM messages WHERE postId = ? ORDER BY createdAt ASC LIMIT ?`,
    [postId, limit]
  );
  return rows;
}

async function deleteMessagesByPost(postId) {
  await query('DELETE FROM messages WHERE postId = ?', [postId]);
}
```

- [ ] **Step 3: 将三个函数加入 `module.exports`**

将 `module.exports` 改为：

```js
module.exports = {
  dbConfig,
  initDb,
  mapPost,
  query,
  withTransaction,
  getUserRank,
  insertMessage,
  getRecentMessages,
  deleteMessagesByPost
};
```

- [ ] **Step 4: 重启后端验证表已创建**

```bash
cd backend && node -e "require('./src/db').initDb().then(() => { console.log('OK'); process.exit(0); }).catch(e => { console.error(e); process.exit(1); })"
```

Expected: `OK`（无报错）

- [ ] **Step 5: Commit**

```bash
git add backend/src/db.js
git commit -m "feat(db): add messages table and query helpers"
```

---

## Task 3: 后端 — 创建 chat.js WebSocket 服务器

**Files:**
- Create: `backend/src/chat.js`

- [ ] **Step 1: 创建 chat.js**

```js
const { WebSocketServer } = require('ws');
const { query, insertMessage, getRecentMessages, deleteMessagesByPost } = require('./db');
const url = require('url');

const MAX_CONTENT_LENGTH = 500;

// rooms: Map<postId, Set<WebSocket>>
const rooms = new Map();

async function isParticipant(postId, userId) {
  const postRows = await query('SELECT publisherId FROM posts WHERE id = ?', [postId]);
  if (!postRows[0]) return false;
  if (postRows[0].publisherId === userId) return true;
  const buddyRows = await query(
    'SELECT id FROM post_buddies WHERE postId = ? AND userId = ?',
    [postId, userId]
  );
  return buddyRows.length > 0;
}

function broadcast(postId, data) {
  const room = rooms.get(postId);
  if (!room) return;
  const msg = JSON.stringify(data);
  for (const ws of room) {
    if (ws.readyState === ws.OPEN) ws.send(msg);
  }
}

function addToRoom(postId, ws) {
  if (!rooms.has(postId)) rooms.set(postId, new Set());
  rooms.get(postId).add(ws);
}

function removeFromRoom(postId, ws) {
  const room = rooms.get(postId);
  if (!room) return;
  room.delete(ws);
  if (room.size === 0) rooms.delete(postId);
}

function closeRoom(postId, reason) {
  const room = rooms.get(postId);
  if (!room) return;
  const msg = JSON.stringify({ type: 'room_closed', reason });
  for (const ws of room) {
    if (ws.readyState === ws.OPEN) {
      ws.send(msg);
      ws.close(1000, 'room closed');
    }
  }
  rooms.delete(postId);
}

function createChatServer(httpServer) {
  const wss = new WebSocketServer({ server: httpServer, path: '/chat' });

  wss.on('connection', async (ws, req) => {
    const params = new url.URL(req.url, 'http://localhost').searchParams;
    const postId = params.get('postId');
    const userId = params.get('userId');

    if (!postId || !userId) {
      ws.close(4001, 'missing postId or userId');
      return;
    }

    let participant;
    try {
      participant = await isParticipant(postId, userId);
    } catch {
      ws.close(4001, 'auth error');
      return;
    }

    if (!participant) {
      ws.close(4001, 'not a participant');
      return;
    }

    const userRows = await query('SELECT nickname FROM users WHERE id = ?', [userId]);
    const senderName = userRows[0] ? userRows[0].nickname : userId;

    addToRoom(postId, ws);

    ws.on('message', async (raw) => {
      let parsed;
      try { parsed = JSON.parse(raw); } catch { return; }
      if (parsed.type !== 'message') return;

      const content = String(parsed.content || '').trim();
      if (!content || content.length > MAX_CONTENT_LENGTH) return;

      let insertId;
      try {
        insertId = await insertMessage(postId, userId, senderName, content);
      } catch (e) {
        console.error('[chat] insertMessage error:', e);
        return;
      }

      broadcast(postId, {
        type: 'message',
        id: insertId,
        senderId: userId,
        senderName,
        content,
        createdAt: new Date().toISOString()
      });
    });

    ws.on('close', () => removeFromRoom(postId, ws));
    ws.on('error', () => removeFromRoom(postId, ws));
  });

  return { closeRoom };
}

module.exports = { createChatServer, closeRoom, deleteMessagesByPost };
```

- [ ] **Step 2: 语法检查**

```bash
node -e "require('./backend/src/chat.js'); console.log('OK')"
```

Expected: `OK`

- [ ] **Step 3: Commit**

```bash
git add backend/src/chat.js
git commit -m "feat(chat): add WebSocket chat server with room management"
```

---

## Task 4: 后端 — server.js 升级为 http.createServer + 挂载 chat + 历史消息接口

**Files:**
- Modify: `backend/src/server.js`

- [ ] **Step 1: 在文件顶部引入 http 和 chat 模块**

在 `backend/src/server.js` 顶部，紧接 `const fs = require('fs');` 之后添加：

```js
const http = require('http');
const { createChatServer } = require('./chat');
const { getRecentMessages, deleteMessagesByPost } = require('./db');
```

- [ ] **Step 2: 添加历史消息 REST 接口**

在 `app.get('/api/health', ...)` 之后添加：

```js
app.get('/api/chat/:postId/history', async (req, res, next) => {
  try {
    const { postId } = req.params;
    const { userId } = req.query;
    if (!userId) return res.status(400).json({ message: '缺少 userId' });

    const postRows = await query('SELECT * FROM posts WHERE id = ?', [postId]);
    const post = postRows[0];
    if (!post) return res.status(404).json({ message: '帖子不存在' });
    if (!post.partnerChat) return res.status(403).json({ message: '该任务未开启聊天' });

    const ok = await isParticipant(postId, userId, post);
    if (!ok) return res.status(403).json({ message: '只有参与者可以查看聊天记录' });

    const messages = await getRecentMessages(postId, 50);
    res.json({ messages });
  } catch (error) {
    next(error);
  }
});
```

- [ ] **Step 3: 在 complete 路由末尾（`res.json(mapPost(...))` 之前）添加消息清理调用**

找到 `app.post('/api/posts/:id/complete', ...)` 路由，在 `res.json(mapPost(freshRows[0]));` 之前插入：

```js
    await deleteMessagesByPost(req.params.id);
    chatServer.closeRoom(req.params.id, 'task_completed');
```

完整修改后的路由体如下（替换整个路由）：

```js
app.post('/api/posts/:id/complete', async (req, res, next) => {
  try {
    await syncPostStatus(req.params.id);
    const { userId } = req.body;
    const rows = await query('SELECT * FROM posts WHERE id = ?', [req.params.id]);
    const current = rows[0];

    if (!current) return res.status(404).json({ message: '帖子不存在' });
    if (current.status !== '进行中') return res.status(400).json({ message: '只有进行中的任务才能标记完成' });
    if (userId && current.publisherId !== userId) return res.status(403).json({ message: '只有发布者可以标记完成' });

    await query('UPDATE posts SET status = ? WHERE id = ?', ['待评价', req.params.id]);
    await deleteMessagesByPost(req.params.id);
    chatServer.closeRoom(req.params.id, 'task_completed');
    const freshRows = await query('SELECT * FROM posts WHERE id = ?', [req.params.id]);
    res.json(mapPost(freshRows[0]));
  } catch (error) {
    next(error);
  }
});
```

- [ ] **Step 4: 在 abandon 路由末尾同样添加消息清理**

找到 `app.post('/api/posts/:id/abandon', ...)` 路由，在 `res.json(mapPost(freshRows[0]));` 之前插入：

```js
    await deleteMessagesByPost(req.params.id);
    chatServer.closeRoom(req.params.id, 'task_abandoned');
```

完整修改后的路由体如下（替换整个路由）：

```js
app.post('/api/posts/:id/abandon', async (req, res, next) => {
  try {
    await syncPostStatus(req.params.id);
    const { userId } = req.body;
    if (!userId) return res.status(400).json({ message: '缺少 userId' });

    const postRows = await query('SELECT * FROM posts WHERE id = ?', [req.params.id]);
    const post = postRows[0];
    if (!post) return res.status(404).json({ message: '帖子不存在' });
    if (post.publisherId !== userId) return res.status(403).json({ message: '只有发布者可以放弃任务' });
    if (post.status !== '招募中' && post.status !== '进行中') return res.status(400).json({ message: '当前状态无法放弃' });

    await withTransaction(async (connection) => {
      await connection.execute('UPDATE posts SET status = ? WHERE id = ?', ['已放弃', req.params.id]);
      await connection.execute(
        'UPDATE users SET points = GREATEST(0, points - ?) WHERE id = ?',
        [post.penalty, post.publisherId]
      );
      await insertPointLog(connection, post.publisherId, -post.penalty, `放弃任务《${post.title}》`);
    });

    await recalcCompletionRate(post.publisherId);
    await deleteMessagesByPost(req.params.id);
    chatServer.closeRoom(req.params.id, 'task_abandoned');
    const freshRows = await query('SELECT * FROM posts WHERE id = ?', [req.params.id]);
    res.json(mapPost(freshRows[0]));
  } catch (error) {
    next(error);
  }
});
```

- [ ] **Step 5: 将 `startServer` 改为 http.createServer**

找到 `startServer` 函数，将：

```js
async function startServer() {
  try {
    await initDb();
    app.listen(PORT, () => {
      console.log(`Task Buddy backend listening on http://localhost:${PORT}`);
    });
  } catch (error) {
    console.error('Failed to start backend:', error);
    process.exit(1);
  }
}
```

替换为：

```js
async function startServer() {
  try {
    await initDb();
    const httpServer = http.createServer(app);
    const chatServer = createChatServer(httpServer);
    // expose chatServer so routes can call chatServer.closeRoom
    app.locals.chatServer = chatServer;
    httpServer.listen(PORT, () => {
      console.log(`Task Buddy backend listening on http://localhost:${PORT}`);
    });
  } catch (error) {
    console.error('Failed to start backend:', error);
    process.exit(1);
  }
}
```

- [ ] **Step 6: 在 complete / abandon 路由中使用 `req.app.locals.chatServer`**

注意：上面 Task 4 Step 3-4 中的 `chatServer.closeRoom(...)` 需要改为 `req.app.locals.chatServer.closeRoom(...)`。
请检查刚才添加的两处调用，将其改为：

```js
req.app.locals.chatServer.closeRoom(req.params.id, 'task_completed');
// 以及
req.app.locals.chatServer.closeRoom(req.params.id, 'task_abandoned');
```

- [ ] **Step 7: 验证服务器正常启动**

```bash
cd backend && node src/server.js &
sleep 2
curl http://localhost:3000/api/health
kill %1
```

Expected: `{"success":true,"message":"backend is running"}`

- [ ] **Step 8: Commit**

```bash
git add backend/src/server.js
git commit -m "feat(server): attach WebSocket chat server and add chat history endpoint"
```

---

## Task 5: 前端 — 注册 chat 页面路由

**Files:**
- Modify: `miniprogram/app.json`

- [ ] **Step 1: 在 pages 数组末尾添加 chat 页面**

在 `miniprogram/app.json` 的 `"pages"` 数组末尾追加：

```json
"pages/chat/chat"
```

完整 pages 数组：

```json
"pages": [
  "pages/login/login",
  "pages/home/home",
  "pages/publish/publish",
  "pages/ranking/ranking",
  "pages/profile/profile",
  "pages/post-detail/post-detail",
  "pages/score-history/score-history",
  "pages/admin/admin",
  "pages/evaluate/evaluate",
  "pages/submit-evidence/submit-evidence",
  "pages/chat/chat"
]
```

- [ ] **Step 2: Commit**

```bash
git add miniprogram/app.json
git commit -m "feat(miniprogram): register chat page route"
```

---

## Task 6: 前端 — getChatHistory API 函数

**Files:**
- Modify: `miniprogram/services/api.js`

- [ ] **Step 1: 在 `module.exports` 之前添加函数**

```js
function getChatHistory(postId, userId) {
  return request({ url: `/api/chat/${postId}/history?userId=${encodeURIComponent(userId)}` });
}
```

- [ ] **Step 2: 将 `getChatHistory` 加入 `module.exports`**

在 `module.exports` 对象末尾添加 `getChatHistory`。

- [ ] **Step 3: Commit**

```bash
git add miniprogram/services/api.js
git commit -m "feat(api): add getChatHistory function"
```

---

## Task 7: 前端 — post-detail 增加进入聊天室入口

**Files:**
- Modify: `miniprogram/pages/post-detail/post-detail.wxml`
- Modify: `miniprogram/pages/post-detail/post-detail.js`

- [ ] **Step 1: 在 post-detail.wxml 的操作按钮区末尾添加聊天室按钮**

在 `</view><!-- 操作按钮区 -->` 的最后一个 `</button>` 之后、`</view>` 之前插入：

```xml
<!-- 聊天室入口 -->
<button
  wx:if="{{canOpenChat}}"
  class="ghost-btn action-btn"
  bindtap="openChat"
>进入聊天室</button>
```

- [ ] **Step 2: 在 post-detail.js 的 data 中添加 canOpenChat**

在 `data` 对象的 `canDeleteActive: false` 之后添加：

```js
canOpenChat: false,
```

- [ ] **Step 3: 在 `_loadDetail` 的 `setData` 调用中计算并设置 canOpenChat**

在 `_loadDetail` 函数里，`const isParticipant = isPublisher || isBuddy;` 这行之后添加：

```js
const canOpenChat = post.partnerChat &&
  isParticipant &&
  post.status !== '已完成' &&
  post.status !== '已放弃';
```

然后在 `this.setData({...})` 调用中加入 `canOpenChat`。

- [ ] **Step 4: 添加 openChat 方法**

在 `deleteActiveAnnotation` 方法之后添加：

```js
openChat() {
  wx.navigateTo({
    url: `/pages/chat/chat?postId=${this.data.post.id}`
  });
},
```

- [ ] **Step 5: Commit**

```bash
git add miniprogram/pages/post-detail/post-detail.wxml miniprogram/pages/post-detail/post-detail.js
git commit -m "feat(post-detail): add chat room entry button"
```

---

## Task 8: 前端 — 创建聊天室页面

**Files:**
- Create: `miniprogram/pages/chat/chat.json`
- Create: `miniprogram/pages/chat/chat.wxml`
- Create: `miniprogram/pages/chat/chat.wxss`
- Create: `miniprogram/pages/chat/chat.js`

- [ ] **Step 1: 创建 chat.json**

```json
{
  "navigationBarTitleText": "任务聊天室",
  "navigationBarBackgroundColor": "#fff7ed",
  "navigationBarTextStyle": "black",
  "backgroundColor": "#fffaf4"
}
```

- [ ] **Step 2: 创建 chat.wxml**

```xml
<view class="container">
  <scroll-view
    class="message-list"
    scroll-y
    scroll-into-view="{{scrollTarget}}"
    scroll-with-animation
  >
    <view wx:if="{{messages.length === 0 && !isClosed}}" class="empty-hint">暂无消息，发送第一条吧</view>

    <block wx:for="{{messages}}" wx:key="id">
      <view class="msg-row {{item.senderId === currentUserId ? 'msg-row-self' : ''}}">
        <view class="bubble {{item.senderId === currentUserId ? 'bubble-self' : 'bubble-other'}}">
          <view wx:if="{{item.senderId !== currentUserId}}" class="sender-name">{{item.senderName}}</view>
          <view class="msg-content">{{item.content}}</view>
          <view class="msg-time">{{item.timeStr}}</view>
        </view>
      </view>
    </block>

    <view id="msg-bottom" />
  </scroll-view>

  <view wx:if="{{isClosed}}" class="closed-banner">聊天室已关闭</view>

  <view class="input-bar" wx:if="{{!isClosed}}">
    <input
      class="msg-input"
      value="{{inputText}}"
      bindinput="onInput"
      placeholder="输入文字（最多500字）"
      placeholder-class="placeholder"
      maxlength="500"
      confirm-type="send"
      bindconfirm="sendMessage"
    />
    <button class="send-btn" bindtap="sendMessage">发送</button>
  </view>
</view>
```

- [ ] **Step 3: 创建 chat.wxss**

```css
.container {
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: #fffaf4;
}

.message-list {
  flex: 1;
  padding: 16rpx 24rpx;
  box-sizing: border-box;
}

.empty-hint {
  text-align: center;
  color: #aaa;
  font-size: 26rpx;
  margin-top: 60rpx;
}

.msg-row {
  display: flex;
  margin-bottom: 24rpx;
}

.msg-row-self {
  justify-content: flex-end;
}

.bubble {
  max-width: 68%;
  padding: 16rpx 20rpx;
  border-radius: 16rpx;
  word-break: break-all;
}

.bubble-other {
  background: #fff;
  border: 1rpx solid #ede8e8;
}

.bubble-self {
  background: #ff7a59;
  color: #fff;
}

.sender-name {
  font-size: 22rpx;
  color: #888;
  margin-bottom: 6rpx;
}

.msg-content {
  font-size: 30rpx;
  line-height: 1.5;
}

.msg-time {
  font-size: 20rpx;
  color: #bbb;
  margin-top: 6rpx;
  text-align: right;
}

.bubble-self .msg-time {
  color: rgba(255,255,255,0.7);
}

.closed-banner {
  text-align: center;
  padding: 20rpx;
  color: #e11d48;
  font-size: 28rpx;
  background: #fff0f4;
  border-top: 1rpx solid #fcc;
}

.input-bar {
  display: flex;
  align-items: center;
  padding: 16rpx 24rpx;
  background: #fff;
  border-top: 1rpx solid #f0e8e0;
}

.msg-input {
  flex: 1;
  height: 72rpx;
  background: #f5f0eb;
  border-radius: 36rpx;
  padding: 0 24rpx;
  font-size: 28rpx;
  margin-right: 16rpx;
}

.send-btn {
  background: #ff7a59;
  color: #fff;
  font-size: 28rpx;
  padding: 0 32rpx;
  height: 72rpx;
  line-height: 72rpx;
  border-radius: 36rpx;
  min-width: 120rpx;
}

.placeholder {
  color: #bbb;
}
```

- [ ] **Step 4: 创建 chat.js**

```js
const api = require('../../services/api');
const config = require('../../env');

function formatTime(val) {
  const d = new Date(val);
  if (isNaN(d.getTime())) return '';
  const pad = n => String(n).padStart(2, '0');
  return `${pad(d.getHours())}:${pad(d.getMinutes())}`;
}

Page({
  data: {
    messages: [],
    inputText: '',
    isClosed: false,
    currentUserId: '',
    postId: '',
    scrollTarget: ''
  },

  _postId: '',
  _socketOpen: false,
  _reconnectCount: 0,
  _maxReconnect: 3,

  async onLoad(options) {
    const app = getApp();
    const userInfo = app.globalData.userInfo || wx.getStorageSync('userInfo');
    const currentUserId = userInfo ? userInfo.id : '';
    const postId = options.postId || '';
    this._postId = postId;
    this.setData({ currentUserId, postId });

    try {
      const { messages } = await api.getChatHistory(postId, currentUserId);
      const formatted = (messages || []).map(m => ({
        ...m,
        timeStr: formatTime(m.createdAt)
      }));
      this.setData({ messages: formatted, scrollTarget: 'msg-bottom' });
    } catch (e) {
      wx.showToast({ title: '加载历史消息失败', icon: 'none' });
    }

    this._connect();
  },

  _connect() {
    const { currentUserId } = this.data;
    const postId = this._postId;
    const wsBase = config.apiBaseUrl.replace(/^http/, 'ws');
    const wsUrl = `${wsBase}/chat?postId=${encodeURIComponent(postId)}&userId=${encodeURIComponent(currentUserId)}`;

    wx.connectSocket({ url: wsUrl });

    wx.onSocketOpen(() => {
      this._socketOpen = true;
      this._reconnectCount = 0;
    });

    wx.onSocketMessage((res) => {
      let msg;
      try { msg = JSON.parse(res.data); } catch { return; }

      if (msg.type === 'room_closed') {
        this._socketOpen = false;
        wx.closeSocket();
        this.setData({ isClosed: true });
        return;
      }

      if (msg.type === 'message') {
        const newMsg = { ...msg, timeStr: formatTime(msg.createdAt) };
        this.setData({
          messages: [...this.data.messages, newMsg],
          scrollTarget: 'msg-bottom'
        });
      }
    });

    wx.onSocketClose(() => {
      this._socketOpen = false;
      if (!this.data.isClosed && this._reconnectCount < this._maxReconnect) {
        this._reconnectCount++;
        setTimeout(() => this._connect(), 2000);
      }
    });

    wx.onSocketError(() => {
      this._socketOpen = false;
    });
  },

  onInput(e) {
    this.setData({ inputText: e.detail.value });
  },

  sendMessage() {
    const content = String(this.data.inputText || '').trim();
    if (!content) return;
    if (content.length > 500) {
      wx.showToast({ title: '消息不能超过500字', icon: 'none' });
      return;
    }
    if (!this._socketOpen) {
      wx.showToast({ title: '连接已断开，请稍后重试', icon: 'none' });
      return;
    }
    wx.sendSocketMessage({ data: JSON.stringify({ type: 'message', content }) });
    this.setData({ inputText: '' });
  },

  onUnload() {
    if (this._socketOpen) {
      wx.closeSocket();
    }
  }
});
```

- [ ] **Step 5: Commit**

```bash
git add miniprogram/pages/chat/
git commit -m "feat(chat): add chat room page with WebSocket support"
```

---

## Task 9: 端到端验证

- [ ] **Step 1: 启动后端**

```bash
cd backend && node src/server.js
```

Expected: `Task Buddy backend listening on http://localhost:3000`

- [ ] **Step 2: 确认 WebSocket 端口可连接**

```bash
curl http://localhost:3000/api/health
```

Expected: `{"success":true,"message":"backend is running"}`

- [ ] **Step 3: 在微信开发者工具中打开项目，编译运行**

手动测试流程：
1. 以用户 A 登录，发布一个开启了"开放搭子聊天"的任务
2. 以用户 B 登录，在帖子详情页加入任务
3. 两个用户各自在帖子详情页看到"进入聊天室"按钮
4. 进入聊天室，互发消息，确认双方实时收到
5. 用户 A 在帖子详情页点击"放弃任务"
6. 确认聊天室显示"聊天室已关闭"，输入框消失

- [ ] **Step 4: 最终 Commit（如有遗漏文件）**

```bash
git status
git add -A
git commit -m "feat: task-scoped chat room complete"
```
